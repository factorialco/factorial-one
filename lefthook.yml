# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: pnpm exec commitlint --edit {1}
pre-commit:
  commands:
    format:
      exclude:
        - ".prettierignore"
      run: pnpm exec prettier {staged_files} --write
      stage_fixed: true
