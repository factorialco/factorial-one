---
description: "F0 DS - Component Guidelines"
globs: "packages/react/src/components/**/*.tsx"
alwaysApply: true
---

# Component Guidelines

- Import only used React components and functions. NEVER import entire React via `import * as React from "react"`.
- Use named exports for all components.
- Implement `'use client'` directive only when necessary.
- Structure component files:
  1. **Type definitions and interfaces**
  2. **Component implementation**
  3. **Helper functions**
  4. **Exports**
