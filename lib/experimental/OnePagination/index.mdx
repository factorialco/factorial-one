import { Canvas, Meta, Unstyled, Controls } from "@storybook/blocks"
import * as PaginationStories from "./index.stories"

<Meta of={PaginationStories} />

# Pagination

Divides content into separate pages, allowing users to navigate through large
sets of data or items one page at a time.

```tsx
import { OnePagination } from "@factorialco/factorial-one/dist/experimental"
```

## Anatomy

<Canvas of={PaginationStories.Default} />
<Controls of={PaginationStories.Default} />

## Examples

### Long list

<Canvas of={PaginationStories.LongList} />

### Without Controls

<Canvas of={PaginationStories.WithoutControls} />
